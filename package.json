{
  "name": "obsidian-outlook-mailbox",
  "version": "0.1.0",
  "description": "Sync Microsoft 365 (Outlook) mail into your Obsidian vault as one note per email plus a per-folder thread index.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "install:vault": "node scripts/install.mjs"
  },
  "keywords": [
    "obsidian",
    "outlook",
    "microsoft-365",
    "email",
    "graph"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/turndown": "^5.0.6",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.7",
    "tslib": "^2.6.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "turndown": "^7.1.2"
  }
}
